{
  "build": {
    "cmd": "composer install && php artisan config:clear"
  },
  "start": {
    "cmd": "php artisan config:clear && php artisan route:clear && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
